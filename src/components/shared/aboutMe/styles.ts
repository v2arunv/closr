import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    row: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 10,
    },
    icon: {
        width: 20,
        height: 20,
    },
    rowText: {
        marginLeft: 10,
    },
    lastRow: {
        marginBottom: 10,
    }

});

export default styles